# Klein UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Создать параллельный мобильно-оптимизированный Gradio-интерфейс на порту 7861 для Flux.2-Klein img2img workflow поверх существующего Forge-Neo, без изменения оригинальных файлов.

**Architecture:** `RUN-Klein.bat` запускает цепочку bat-файлов → `launch_klein.py` (патч `start()` → `webui_klein`) → `webui_klein.py` (полная инициализация Forge) → `modules_klein/ui_klein.py` строит img2img + extras блоки напрямую из тех же модулей что и оригинал, собирает в Gradio app с bottom-nav CSS. Оригинальные файлы Forge не трогаются.

**Tech Stack:** Python 3.11, Gradio 4.x, CSS3 (mobile-first, CSS custom properties, glassmorphism), Windows batch scripts, Playwright (для smoke-тестов)

**Spec:** `docs/superpowers/specs/2026-03-23-klein-ui-design.md`

---

## File Map

| Файл | Действие | Ответственность |
|---|---|---|
| `H:\Forge-Neo-v3.2-sage\RUN-Klein.bat` | Create | Top-level launcher |
| `webui\webui-user-klein.bat` | Create | COMMANDLINE_ARGS с --port 7861 |
| `webui\webui-klein.bat` | Create | Копия webui.bat с launch_klein.py |
| `webui\launch_klein.py` | Create | Патч start() + делегирует main() |
| `webui\webui_klein.py` | Create | Worker: init + запуск Klein Gradio app |
| `webui\modules_klein\__init__.py` | Create | Пакет (пустой) |
| `webui\modules_klein\klein.css` | Create | Design system: CSS custom properties + mobile layout |
| `webui\modules_klein\klein_js.py` | Create | JavaScript-строки: bottom nav, presets popup, collapsible model bar |
| `webui\modules_klein\ui_klein.py` | Create | Главный UI: img2img + extras blocks + сборка |

---

## Task 1: Launch Chain (bat files)

**Files:**
- Create: `H:\Forge-Neo-v3.2-sage\RUN-Klein.bat`
- Create: `H:\Forge-Neo-v3.2-sage\webui\webui-user-klein.bat`
- Create: `H:\Forge-Neo-v3.2-sage\webui\webui-klein.bat`

- [ ] **Step 1: Создать RUN-Klein.bat**

```bat
@echo off
call environment.bat
cd %~dp0webui
call webui-user-klein.bat
pause
```

Путь: `H:\Forge-Neo-v3.2-sage\RUN-Klein.bat`

- [ ] **Step 2: Создать webui-user-klein.bat**

```bat
@echo off
set COMMANDLINE_ARGS=--sage --uv --pin-shared-memory --cuda-malloc --cuda-stream --listen --enable-insecure-extension-access --api --port 7861
call webui-klein.bat
```

Путь: `H:\Forge-Neo-v3.2-sage\webui\webui-user-klein.bat`

- [ ] **Step 3: Создать webui-klein.bat**

Скопировать `webui.bat` полностью, заменить строку `:launch` секции:
```bat
:launch
%PYTHON% launch_klein.py %*
```
(было `launch.py`, стало `launch_klein.py`)

Путь: `H:\Forge-Neo-v3.2-sage\webui\webui-klein.bat`

- [ ] **Step 4: Smoke-тест цепочки bat**

```bash
# Проверить что файлы существуют
ls "H:/Forge-Neo-v3.2-sage/RUN-Klein.bat"
ls "H:/Forge-Neo-v3.2-sage/webui/webui-user-klein.bat"
ls "H:/Forge-Neo-v3.2-sage/webui/webui-klein.bat"

# Проверить что webui-klein.bat содержит launch_klein.py
grep "launch_klein" "H:/Forge-Neo-v3.2-sage/webui/webui-klein.bat"
```

Ожидается: все файлы найдены, grep возвращает строку с `launch_klein.py`

- [ ] **Step 5: Commit**

```bash
cd "H:/Forge-Neo-v3.2-sage"
git add RUN-Klein.bat webui/webui-user-klein.bat webui/webui-klein.bat
git commit -m "feat: add Klein UI launch chain bat files"
```

---

## Task 2: launch_klein.py + webui_klein.py

**Files:**
- Create: `H:\Forge-Neo-v3.2-sage\webui\launch_klein.py`
- Create: `H:\Forge-Neo-v3.2-sage\webui\webui_klein.py`

- [ ] **Step 1: Создать launch_klein.py**

```python
# launch_klein.py
# Identical to launch.py except start() is patched to launch webui_klein

from modules import launch_utils

args = launch_utils.args
python = launch_utils.python
git = launch_utils.git
index_url = launch_utils.index_url
dir_repos = launch_utils.dir_repos

if args.uv or args.uv_symlink:
    from modules_forge.uv_hook import patch
    patch(args.uv_symlink)

git_tag = launch_utils.git_tag
run = launch_utils.run
is_installed = launch_utils.is_installed
repo_dir = launch_utils.repo_dir
run_pip = launch_utils.run_pip
check_run_python = launch_utils.check_run_python
git_clone = launch_utils.git_clone
git_pull_recursive = launch_utils.git_pull_recursive
list_extensions = launch_utils.list_extensions
run_extension_installer = launch_utils.run_extension_installer
prepare_environment = launch_utils.prepare_environment


def start():
    """Override: launch webui_klein instead of webui."""
    import sys
    print(f"Launching Klein UI with arguments: {' '.join(sys.argv[1:])}")

    from modules import logging_config
    logging_config.setup_logging(args.loglevel)

    import webui_klein

    if "--nowebui" in sys.argv:
        webui_klein.api_only()
    else:
        webui_klein.webui()

    from modules_forge import main_thread
    main_thread.loop()


def main():
    if args.dump_sysinfo:
        filename = launch_utils.dump_sysinfo()
        print(f"Sysinfo saved as {filename}. Exiting...")
        exit(0)

    launch_utils.verify_version()
    launch_utils.startup_timer.record("initial startup")

    with launch_utils.startup_timer.subcategory("prepare environment"):
        if not args.skip_prepare_environment:
            prepare_environment()

    if args.forge_ref_a1111_home:
        launch_utils.configure_a1111_reference(args.forge_ref_a1111_home)
    if args.forge_ref_comfy_home:
        launch_utils.configure_comfy_reference(args.forge_ref_comfy_home)
    if args.forge_ref_comfy_yaml:
        launch_utils.configure_comfy_yaml(args.forge_ref_comfy_yaml)

    start()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Создать webui_klein.py**

```python
# webui_klein.py
# Klein UI worker — mirrors webui.py but uses modules_klein.ui_klein

if __name__ == "__main__":
    raise SystemError("Call launch_klein.py instead")

import os
import time
from threading import Thread

from fastapi import Request
from fastapi.encoders import jsonable_encoder
from fastapi.responses import JSONResponse

from modules import initialize, initialize_util, timer
from modules_forge.initialization import initialize_forge

startup_timer = timer.startup_timer
startup_timer.record("launcher")

initialize.shush()

with startup_timer.subcategory("forge init"):
    initialize_forge()

initialize.imports()
initialize.check_versions()
initialize.initialize()


def _handle_exception(request: Request, e: Exception):
    error_information = vars(e)
    content = {
        "error": type(e).__name__,
        "detail": error_information.get("detail", ""),
        "body": error_information.get("body", ""),
        "message": str(e),
    }
    return JSONResponse(
        status_code=int(error_information.get("status_code", 500)),
        content=jsonable_encoder(content),
    )


def create_api(app):
    from modules.api.api import Api
    from modules.call_queue import queue_lock
    api = Api(app, queue_lock)
    return api


def api_only_worker():
    from fastapi import FastAPI
    from modules.shared_cmd_options import cmd_opts

    app = FastAPI(exception_handlers={Exception: _handle_exception})
    initialize_util.setup_middleware(app)
    api = create_api(app)

    from modules import script_callbacks
    script_callbacks.before_ui_callback()
    script_callbacks.app_started_callback(None, app)

    print(f"Klein UI startup time: {startup_timer.summary()}.")
    api.launch(
        server_name=initialize_util.gradio_server_name(),
        port=cmd_opts.port if cmd_opts.port else 7861,
        root_path=f"/{cmd_opts.subpath}" if cmd_opts.subpath else "",
    )


def webui_worker():
    from modules.shared_cmd_options import cmd_opts
    from modules import (
        progress,
        script_callbacks,
        shared,
        ui_extra_networks,
        ui_tempdir,
    )
    from modules_klein import ui_klein  # ← Klein UI instead of modules.ui

    while 1:
        if shared.opts.clean_temp_dir_at_start:
            ui_tempdir.cleanup_tmpdr()
            startup_timer.record("cleanup temp dir")

        script_callbacks.before_ui_callback()
        startup_timer.record("scripts before_ui_callback")

        shared.demo = ui_klein.create_klein_ui()  # ← Klein UI
        startup_timer.record("create klein ui")

        if not cmd_opts.no_gradio_queue:
            shared.demo.queue(default_concurrency_limit=32)

        gradio_auth_creds = list(initialize_util.get_gradio_auth_creds()) or None

        auto_launch_browser = False
        if os.getenv("SD_WEBUI_RESTARTING") != "1":
            if shared.opts.auto_launch_browser == "Remote" or cmd_opts.autolaunch:
                auto_launch_browser = True
            elif shared.opts.auto_launch_browser == "Local":
                auto_launch_browser = not cmd_opts.webui_is_non_local

        from modules_forge.forge_canvas.canvas import canvas_js_root_path

        app, local_url, share_url = shared.demo.launch(
            share=cmd_opts.share,
            server_name=initialize_util.gradio_server_name(),
            server_port=cmd_opts.port,
            ssl_keyfile=cmd_opts.tls_keyfile,
            ssl_certfile=cmd_opts.tls_certfile,
            ssl_verify=cmd_opts.disable_tls_verify,
            debug=cmd_opts.gradio_debug,
            auth=gradio_auth_creds,
            inbrowser=auto_launch_browser,
            prevent_thread_lock=True,
            allowed_paths=cmd_opts.gradio_allowed_path + [canvas_js_root_path],
            app_kwargs={
                "docs_url": "/docs",
                "redoc_url": "/redoc",
                "exception_handlers": {Exception: _handle_exception},
            },
            root_path=f"/{cmd_opts.subpath}" if cmd_opts.subpath else "",
        )

        startup_timer.record("gradio launch")

        app.user_middleware = [
            x for x in app.user_middleware if x.cls.__name__ != "CORSMiddleware"
        ]
        initialize_util.setup_middleware(app)
        progress.setup_progress_api(app)

        if cmd_opts.api:
            create_api(app)

        ui_extra_networks.add_pages_to_demo(app)
        startup_timer.record("add APIs")

        with startup_timer.subcategory("app_started_callback"):
            script_callbacks.app_started_callback(shared.demo, app)

        timer.startup_record = startup_timer.dump()
        print(f"Klein UI startup time: {startup_timer.summary()}.")

        try:
            while True:
                server_command = shared.state.wait_for_server_command(timeout=5)
                if server_command:
                    if server_command in ("stop", "restart"):
                        break
                    else:
                        print(f"Unknown server command: {server_command}")
        except KeyboardInterrupt:
            print("Caught KeyboardInterrupt, stopping Klein UI...")
            server_command = "stop"

        if server_command == "stop":
            print("Stopping Klein UI...")
            shared.demo.close()
            break

        os.environ.setdefault("SD_WEBUI_RESTARTING", "1")
        print("Restarting Klein UI...")
        shared.demo.close()
        time.sleep(0.5)
        startup_timer.reset()
        script_callbacks.app_reload_callback()
        startup_timer.record("app reload callback")
        script_callbacks.script_unloaded_callback()
        startup_timer.record("scripts unloaded callback")
        initialize.initialize_rest(reload_script_modules=True)


def api_only():
    Thread(target=api_only_worker, daemon=True).start()


def webui():
    Thread(target=webui_worker, daemon=True).start()
```

- [ ] **Step 3: Проверить синтаксис файлов**

```bash
python -c "import ast; ast.parse(open('H:/Forge-Neo-v3.2-sage/webui/launch_klein.py').read()); print('launch_klein.py OK')"
python -c "import ast; ast.parse(open('H:/Forge-Neo-v3.2-sage/webui/webui_klein.py').read()); print('webui_klein.py OK')"
```

Ожидается: обе строки печатают `OK`

- [ ] **Step 4: Commit**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add launch_klein.py webui_klein.py
git commit -m "feat: add launch_klein.py and webui_klein.py workers"
```

---

## Task 3: modules_klein package + Design System CSS

**Files:**
- Create: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\__init__.py`
- Create: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\klein.css`
- Create: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\klein_js.py`

- [ ] **Step 1: Создать __init__.py**

```python
# modules_klein/__init__.py
```

Путь: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\__init__.py` (пустой файл)

- [ ] **Step 2: Создать klein.css**

```css
/* klein.css — Klein UI Design System */
/* Mobile-first. Desktop breakpoint: 768px */

:root {
  --accent-primary: #6366f1;
  --accent-secondary: #8b5cf6;
  --accent-gradient: linear-gradient(135deg, #6366f1, #8b5cf6);
  --surface: #0f0f1a;
  --surface-2: rgba(255, 255, 255, 0.03);
  --surface-accent: rgba(99, 102, 241, 0.08);
  --border: rgba(255, 255, 255, 0.06);
  --border-accent: rgba(99, 102, 241, 0.2);
  --text-primary: #e0e0e0;
  --text-muted: #555555;
  --success: #10b981;
  --warning: #f59e0b;
  --nav-height: 56px;
  --safe-bottom: env(safe-area-inset-bottom, 0px);
}

/* ── Base ────────────────────────────────────────── */
.klein-ui {
  background: var(--surface);
  min-height: 100vh;
  padding-bottom: calc(var(--nav-height) + var(--safe-bottom));
}

.klein-ui .gradio-container {
  max-width: 100% !important;
  padding: 0 !important;
}

/* ── Bottom Navigation ───────────────────────────── */
.klein-bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: var(--nav-height);
  padding-bottom: var(--safe-bottom);
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid var(--border);
  display: flex;
  z-index: 1000;
}

.klein-nav-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  font-size: 10px;
  color: var(--text-muted);
  cursor: pointer;
  transition: color 0.2s;
  min-height: 44px;
}

.klein-nav-item.active { color: var(--accent-primary); }
.klein-nav-item svg, .klein-nav-item .nav-icon { font-size: 18px; }

/* ── Model Bar ───────────────────────────────────── */
.klein-model-bar {
  background: var(--surface-accent);
  border-bottom: 1px solid var(--border-accent);
  padding: 8px 12px;
}

.klein-model-bar .collapsed-row {
  display: flex;
  gap: 6px;
  align-items: center;
}

.klein-preset-badge {
  background: var(--accent-gradient);
  border-radius: 5px;
  padding: 3px 9px;
  font-size: 12px;
  font-weight: 600;
  color: white;
  white-space: nowrap;
  min-height: 28px;
  display: flex;
  align-items: center;
}

.klein-model-bar .expanded-row {
  margin-top: 6px;
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
}

/* ── Config + Styles Bar ─────────────────────────── */
.klein-config-bar {
  background: rgba(0, 0, 0, 0.2);
  border-bottom: 1px solid var(--border);
  padding: 6px 12px;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  align-items: center;
}

.klein-style-tag {
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid rgba(139, 92, 246, 0.3);
  border-radius: 4px;
  padding: 2px 8px;
  font-size: 11px;
  color: #c4b5fd;
  display: flex;
  align-items: center;
  gap: 4px;
  min-height: 28px;
}

/* ── Sub-tabs ────────────────────────────────────── */
.klein-subtabs {
  display: flex;
  overflow-x: auto;
  scrollbar-width: none;
  -ms-overflow-style: none;
  border-bottom: 1px solid var(--border);
  background: rgba(0, 0, 0, 0.15);
}

.klein-subtabs::-webkit-scrollbar { display: none; }

.klein-subtabs .tab-nav button {
  flex-shrink: 0;
  white-space: nowrap;
  padding: 8px 14px;
  font-size: 12px;
  background: transparent;
  border: none;
  border-bottom: 2px solid transparent;
  color: var(--text-muted);
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s;
  min-height: 44px;
}

.klein-subtabs .tab-nav button.selected {
  color: var(--accent-primary);
  border-bottom-color: var(--accent-primary);
}

/* ── Param Cards ─────────────────────────────────── */
.klein-param-card {
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 8px 10px;
}

.klein-param-card label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.4px;
  margin-bottom: 4px;
}

/* ── Sliders ─────────────────────────────────────── */
.klein-ui input[type="range"] {
  -webkit-appearance: none;
  height: 4px;
  border-radius: 2px;
  background: rgba(255, 255, 255, 0.08);
  outline: none;
  width: 100%;
}

.klein-ui input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--accent-gradient);
  box-shadow: 0 0 6px rgba(99, 102, 241, 0.5);
  cursor: pointer;
}

/* ── Generate Button ─────────────────────────────── */
.klein-generate-btn button {
  background: var(--accent-gradient) !important;
  border: none !important;
  border-radius: 10px !important;
  padding: 12px !important;
  font-size: 14px !important;
  font-weight: 700 !important;
  color: white !important;
  box-shadow: 0 4px 20px rgba(99, 102, 241, 0.4) !important;
  width: 100% !important;
  min-height: 48px !important;
  transition: transform 0.1s, box-shadow 0.2s !important;
}

.klein-generate-btn button:hover {
  transform: translateY(-1px) !important;
  box-shadow: 0 6px 25px rgba(99, 102, 241, 0.55) !important;
}

.klein-generate-btn button:active {
  transform: translateY(0) !important;
}

/* ── Sticky Generate on Mobile ───────────────────── */
@media (max-width: 767px) {
  .klein-generate-btn {
    position: sticky;
    bottom: calc(var(--nav-height) + var(--safe-bottom) + 8px);
    z-index: 100;
    padding: 0 12px;
    margin-bottom: 4px;
  }
}

/* ── Extensions Container ────────────────────────── */
.klein-extensions {
  contain: layout style;
  overflow: hidden;
}

.klein-extensions > * {
  max-width: 100% !important;
  overflow-x: auto !important;
}

/* Extension injected styles are scoped here — high specificity overrides */
.klein-ui .klein-extensions input,
.klein-ui .klein-extensions select,
.klein-ui .klein-extensions textarea {
  max-width: 100% !important;
}

/* ── Resolution Presets Popup ────────────────────── */
.klein-res-popup {
  position: absolute;
  z-index: 500;
  background: #1a1a2e;
  border: 2px solid var(--accent-primary);
  border-radius: 10px;
  padding: 12px;
  min-width: 220px;
  box-shadow: 0 8px 30px rgba(99, 102, 241, 0.3);
}

.klein-res-popup .res-section-title {
  font-size: 10px;
  color: var(--accent-primary);
  text-align: center;
  margin-bottom: 6px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.klein-res-popup .res-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4px;
  margin-bottom: 8px;
}

.klein-res-popup button {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid var(--border);
  border-radius: 5px;
  padding: 5px;
  font-size: 10px;
  color: #aaa;
  cursor: pointer;
  transition: background 0.2s, color 0.2s;
  min-height: 32px;
}

.klein-res-popup button:hover {
  background: rgba(99, 102, 241, 0.2);
  color: #c0c0f0;
  border-color: var(--border-accent);
}

.klein-res-popup .res-square {
  grid-column: span 2;
  background: rgba(99, 102, 241, 0.15);
  border-color: var(--border-accent);
  color: #a0a0f0;
}

/* ── LoRA Panel ──────────────────────────────────── */
.klein-lora-panel {
  background: rgba(168, 85, 247, 0.06);
  border: 1px solid rgba(168, 85, 247, 0.15);
  border-radius: 8px;
  padding: 8px 10px;
}

/* ── Desktop layout ──────────────────────────────── */
@media (min-width: 768px) {
  .klein-ui {
    padding-bottom: var(--nav-height);
  }

  .klein-main-grid {
    display: grid;
    grid-template-columns: 1fr 320px;
    gap: 0;
    min-height: calc(100vh - 120px);
  }

  .klein-left-col {
    padding: 12px 16px;
    border-right: 1px solid var(--border);
    overflow-y: auto;
  }

  .klein-right-col {
    padding: 12px 16px;
    overflow-y: auto;
  }

  .klein-extensions {
    /* Desktop: extensions inline in right column */
    display: block;
  }
}
```

- [ ] **Step 3: Создать klein_js.py**

```python
# modules_klein/klein_js.py
# JavaScript strings injected into the Klein UI Gradio app

BOTTOM_NAV_JS = """
<script>
(function() {
  // Bottom nav tab switching
  document.addEventListener('DOMContentLoaded', function() {
    const navItems = document.querySelectorAll('.klein-nav-item');
    const sections = {
      'img2img': document.querySelector('#klein-tab-img2img'),
      'extras':  document.querySelector('#klein-tab-extras'),
      'extensions': document.querySelector('#klein-tab-extensions'),
      'settings': document.querySelector('#klein-tab-settings'),
    };

    navItems.forEach(item => {
      item.addEventListener('click', function() {
        const target = this.dataset.tab;
        navItems.forEach(n => n.classList.remove('active'));
        this.classList.add('active');
        Object.entries(sections).forEach(([k, el]) => {
          if (el) el.style.display = k === target ? '' : 'none';
        });
      });
    });
  });
})();
</script>
"""

RESOLUTION_PRESETS_JS = """
<script>
function kleinSetResolution(w, h) {
  // Find width/height number inputs and set values
  const inputs = document.querySelectorAll('#img2img_width input[type=number], #img2img_height input[type=number]');
  if (inputs.length >= 2) {
    inputs[0].value = w;
    inputs[1].value = h;
    inputs[0].dispatchEvent(new Event('input', {bubbles: true}));
    inputs[1].dispatchEvent(new Event('input', {bubbles: true}));
  }
  // Also trigger the range slider
  const sliders = document.querySelectorAll('#img2img_width input[type=range], #img2img_height input[type=range]');
  if (sliders.length >= 2) {
    sliders[0].value = w;
    sliders[1].value = h;
    sliders[0].dispatchEvent(new Event('input', {bubbles: true}));
    sliders[1].dispatchEvent(new Event('input', {bubbles: true}));
  }
  document.getElementById('klein-res-popup').style.display = 'none';
}

function kleinToggleResPopup() {
  const popup = document.getElementById('klein-res-popup');
  if (popup) popup.style.display = popup.style.display === 'none' ? 'block' : 'none';
}

// Close popup on outside click
document.addEventListener('click', function(e) {
  const popup = document.getElementById('klein-res-popup');
  const btn = document.getElementById('klein-res-btn');
  if (popup && btn && !popup.contains(e.target) && !btn.contains(e.target)) {
    popup.style.display = 'none';
  }
});
</script>
"""

COLLAPSIBLE_MODEL_BAR_JS = """
<script>
(function() {
  document.addEventListener('DOMContentLoaded', function() {
    const toggle = document.getElementById('klein-model-bar-toggle');
    const expanded = document.getElementById('klein-model-bar-expanded');
    if (toggle && expanded) {
      toggle.addEventListener('click', function() {
        const isOpen = expanded.style.display !== 'none';
        expanded.style.display = isOpen ? 'none' : '';
        toggle.textContent = isOpen ? '⚙ ▼' : '⚙ ▲';
      });
      // Collapsed by default on mobile
      if (window.innerWidth < 768) {
        expanded.style.display = 'none';
      }
    }
  });
})();
</script>
"""

RESOLUTION_PRESETS_HTML = """
<div id="klein-res-popup" class="klein-res-popup" style="display:none">
  <div class="res-section-title">Square</div>
  <div class="res-grid">
    <button class="res-square" onclick="kleinSetResolution(1024,1024)">1024 × 1024</button>
  </div>
  <div class="res-section-title">Portrait</div>
  <div class="res-grid">
    <button onclick="kleinSetResolution(640,1536)">640 × 1536</button>
    <button onclick="kleinSetResolution(768,1344)">768 × 1344</button>
    <button onclick="kleinSetResolution(832,1216)">832 × 1216</button>
    <button onclick="kleinSetResolution(896,1152)">896 × 1152</button>
  </div>
  <div class="res-section-title">Landscape</div>
  <div class="res-grid">
    <button onclick="kleinSetResolution(1536,640)">1536 × 640</button>
    <button onclick="kleinSetResolution(1344,768)">1344 × 768</button>
    <button onclick="kleinSetResolution(1216,832)">1216 × 832</button>
    <button onclick="kleinSetResolution(1152,896)">1152 × 896</button>
  </div>
</div>
"""

ALL_JS = BOTTOM_NAV_JS + RESOLUTION_PRESETS_JS + COLLAPSIBLE_MODEL_BAR_JS

# Stub: replaced with real implementation in Task 6 (Styles integration)
STYLES_INJECT_JS = ""
```

- [ ] **Step 4: Проверить синтаксис**

```bash
python -c "
import ast
for f in ['modules_klein/__init__.py', 'modules_klein/klein_js.py']:
    ast.parse(open(f'H:/Forge-Neo-v3.2-sage/webui/{f}').read())
    print(f'{f} OK')
"
```

- [ ] **Step 5: Commit**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add modules_klein/
git commit -m "feat: add modules_klein package with CSS design system and JS helpers"
```

---

## Task 4: ui_klein.py — img2img Interface

**Files:**
- Create: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\ui_klein.py`

Это самый большой файл. Копируем блок из `modules/ui.py` строки 483–813.

- [ ] **Step 1: Создать ui_klein.py с заглушкой**

```python
# modules_klein/ui_klein.py
"""
Klein UI — Gradio interface for Flux.2-Klein img2img workflow.
Builds img2img and extras interfaces directly from Forge modules.
No original files are modified.
"""
import gradio as gr

from modules import (
    script_callbacks,
    scripts,
    shared,
    ui_common,
    ui_extra_networks,
    ui_postprocessing,
    ui_toprow,
)
from modules.call_queue import wrap_gradio_call, wrap_gradio_gpu_call, wrap_queued_call
from modules.infotext_utils import PasteField, image_from_url_text
from modules.shared import cmd_opts, opts
from modules.ui_common import create_refresh_button
from modules.ui_components import (
    FormGroup,
    FormHTML,
    FormRow,
    InputAccordion,
    ResizeHandleRow,
    ToolButton,
)
from modules.ui_gradio_extensions import reload_javascript
from modules_forge.forge_canvas.canvas import ForgeCanvas, canvas_head
from modules_klein.klein_js import ALL_JS, RESOLUTION_PRESETS_HTML, STYLES_INJECT_JS

import modules.infotext_utils as parameters_copypaste
import json as _json

# Load CSS
import os as _os
_CSS_PATH = _os.path.join(_os.path.dirname(__file__), 'klein.css')
with open(_CSS_PATH, 'r', encoding='utf-8') as _f:
    KLEIN_CSS = _f.read()


def initialize_scripts():
    """Initialize scripts for img2img only.
    NOTE: scripts_current is NOT reset here — it must stay set during
    _build_img2img_interface() so extension scripts render correctly.
    It is reset in create_klein_ui() after the block is built.
    """
    scripts.scripts_current = scripts.scripts_img2img
    scripts.scripts_img2img.initialize_scripts(is_img2img=True)


def create_klein_ui():
    """
    Build and return the Klein Gradio demo.
    Called from webui_klein.webui_worker() instead of ui.create_ui().
    """
    reload_javascript()
    initialize_scripts()  # sets scripts.scripts_current = scripts.scripts_img2img

    img2img_interface = _build_img2img_interface()
    scripts.scripts_current = None  # reset AFTER build — not before
    extras_interface  = _build_extras_interface()
    klein_demo        = _assemble_klein_demo(img2img_interface, extras_interface)

    return klein_demo


def _build_extras_interface():
    with gr.Blocks(analytics_enabled=False) as extras_interface:
        ui_postprocessing.create_ui()
    return extras_interface


def _assemble_klein_demo(img2img_interface, extras_interface):
    """Wrap img2img + extras in Klein shell with bottom nav and CSS."""
    head = canvas_head + ALL_JS

    with gr.Blocks(
        analytics_enabled=False,
        css=KLEIN_CSS,
        head=head,
        elem_classes=["klein-ui"],
        theme=gr.themes.Base(),
        title="Klein UI",
    ) as klein_demo:
        with gr.Tabs(elem_id="klein-main-tabs"):
            with gr.Tab("img2img", elem_id="klein-tab-img2img"):
                img2img_interface.render()
            with gr.Tab("Extras", elem_id="klein-tab-extras"):
                extras_interface.render()
            with gr.Tab("Extensions", elem_id="klein-tab-extensions"):
                gr.HTML("<p style='color:#666;padding:20px'>Extension controls appear in the img2img tab.</p>")
            with gr.Tab("Settings", elem_id="klein-tab-settings"):
                gr.HTML("<p style='color:#666;padding:20px'>Use main webui (port 7860) for full settings.</p>")

        # Bottom nav HTML overlay
        gr.HTML("""
        <div class="klein-bottom-nav">
          <div class="klein-nav-item active" data-tab="img2img">
            <span class="nav-icon">🖼</span><span>img2img</span>
          </div>
          <div class="klein-nav-item" data-tab="extras">
            <span class="nav-icon">✨</span><span>Extras</span>
          </div>
          <div class="klein-nav-item" data-tab="extensions">
            <span class="nav-icon">🎛</span><span>Ext.</span>
          </div>
          <div class="klein-nav-item" data-tab="settings">
            <span class="nav-icon">⚙</span><span>Settings</span>
          </div>
        </div>
        """)

    return klein_demo


def _build_img2img_interface():
    """
    Build img2img_interface block.
    Based on modules/ui.py lines 483-813 — same code, same elem_ids,
    ensuring Config-Presets JS can find components by id.
    """
    # NOTE TO IMPLEMENTER:
    # Copy the exact block from modules/ui.py starting at line 483:
    #   scripts.scripts_current = scripts.scripts_img2img
    #   scripts.scripts_img2img.initialize_scripts(is_img2img=True)
    #   with gr.Blocks(analytics_enabled=False, head=canvas_head) as img2img_interface:
    #       ...  (all code through line 813)
    #   scripts.scripts_current = None
    #   return img2img_interface
    #
    # IMPORTANT:
    # - Remove the surrounding txt2img context — just the img2img block
    # - Keep ALL elem_id values unchanged (required for Config-Presets JS)
    # - The ui_extra_networks.create_ui() call on line 805 handles LoRA
    # - Wrap the Generate button row with elem_classes=["klein-generate-btn"]
    # - Wrap the parameters section with elem_classes=["klein-param-card"] where appropriate
    # - Add gr.HTML(RESOLUTION_PRESETS_HTML) next to width/height sliders
    # - Add elem_id="klein-res-btn" onclick="kleinToggleResPopup()" button next to W/H

    # PLACEHOLDER — replace with actual copied code from modules/ui.py:483-813
    with gr.Blocks(analytics_enabled=False, head=canvas_head) as img2img_interface:
        gr.HTML("<p style='color:#666;padding:20px'>img2img placeholder — see Task 4 Step 2</p>")
    return img2img_interface
```

- [ ] **Step 2: Проверить что заглушка запускается**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
python -c "
import ast
ast.parse(open('modules_klein/ui_klein.py').read())
print('ui_klein.py syntax OK')
"
```

- [ ] **Step 3: Скопировать img2img блок из modules/ui.py**

Открыть `modules/ui.py` и скопировать строки **483–813** в функцию `_build_img2img_interface()`.

Заменить заглушку на реальный код. Точные изменения при копировании:

1. Убрать первые две строки копируемого блока (они уже в `initialize_scripts()`):
   ```python
   # НЕ копировать (строки 483-484 в ui.py):
   scripts.scripts_current = scripts.scripts_img2img
   scripts.scripts_img2img.initialize_scripts(is_img2img=True)
   ```
   **Также убрать последнюю строку копируемого блока** (строка 813 в ui.py — сброс после build):
   ```python
   # НЕ копировать (строка 813 в ui.py):
   scripts.scripts_current = None
   ```
   Этот сброс уже выполняется в `create_klein_ui()` ПОСЛЕ вызова `_build_img2img_interface()`.
   Если скопировать его внутрь функции, extensions не отрендерятся (scripts_current будет None
   в момент когда Gradio регистрирует компоненты extension-скриптов).

2. Изменить начало блока — убрать внешний `head=canvas_head` (он уже в `_assemble_klein_demo`):
   ```python
   # Было:
   with gr.Blocks(analytics_enabled=False, head=canvas_head) as img2img_interface:
   # Стало:
   with gr.Blocks(analytics_enabled=False) as img2img_interface:
   ```

3. Найти Generate button row и добавить `elem_classes`:
   ```python
   # Найти строку с submit button (примерно ui.py:~750-760)
   # Обернуть в: with gr.Column(elem_classes=["klein-generate-btn"]):
   ```

4. Добавить Resolution Presets кнопку рядом с width/height слайдерами:
   ```python
   # После блока width/height (примерно ui.py:~710-720), добавить:
   gr.HTML(f'<button id="klein-res-btn" onclick="kleinToggleResPopup()" '
           f'class="klein-res-trigger">⊞</button>')
   gr.HTML(RESOLUTION_PRESETS_HTML)
   ```

5. В конце функции добавить `return img2img_interface`

- [ ] **Step 4: Проверить синтаксис после копирования**

```bash
python -c "import ast; ast.parse(open('H:/Forge-Neo-v3.2-sage/webui/modules_klein/ui_klein.py').read()); print('OK')"
```

- [ ] **Step 5: Commit промежуточный**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add modules_klein/ui_klein.py
git commit -m "feat: add ui_klein with img2img interface block"
```

---

## Task 5: Первый запуск и отладка

**Goal:** Klein UI должен открыться в браузере на порту 7861.

- [ ] **Step 1: Запустить RUN-Klein.bat**

```
H:\Forge-Neo-v3.2-sage\RUN-Klein.bat
```

Дождаться строки в консоли: `Running on local URL: http://127.0.0.1:7861`

- [ ] **Step 2: Playwright smoke-тест**

```javascript
// C:/Users/krazz/AppData/Local/Temp/playwright-test-klein.js
const { chromium } = require('playwright');

(async () => {
  const browser = await chromium.launch({ headless: false });
  const page = await browser.newPage();

  await page.setViewportSize({ width: 1920, height: 1080 });
  await page.goto('http://127.0.0.1:7861/', { waitUntil: 'load', timeout: 30000 });
  await page.waitForTimeout(4000);
  await page.screenshot({ path: 'C:/Users/krazz/AppData/Local/Temp/klein-desktop.png' });
  console.log('Desktop screenshot saved');

  // Check img2img tab exists
  const img2imgTab = await page.locator('text=img2img').first();
  const visible = await img2imgTab.isVisible();
  console.log('img2img tab visible:', visible);

  // Mobile
  await page.setViewportSize({ width: 390, height: 844 });
  await page.waitForTimeout(1000);
  await page.screenshot({ path: 'C:/Users/krazz/AppData/Local/Temp/klein-mobile.png' });
  console.log('Mobile screenshot saved');

  await browser.close();
  console.log('Smoke test complete');
})();
```

```bash
cd "C:/Users/krazz/.claude/plugins/cache/playwright-skill/playwright-skill/4.1.0/skills/playwright-skill"
node run.js "C:/Users/krazz/AppData/Local/Temp/playwright-test-klein.js"
```

Ожидается: `img2img tab visible: true`, оба скрина сохранены.

- [ ] **Step 3: Прочитать скрины и проверить**

```bash
# Read screenshots via Read tool and verify layout looks correct
```

- [ ] **Step 4: Исправить найденные ошибки**

Если есть импорт-ошибки или UI не загружается — исправить по логу консоли запуска.
Типичные проблемы:
- `NameError: img2img_tabs` — добавить нужную переменную из ui.py
- `AttributeError: scripts_img2img` — вызвать `initialize_scripts()` раньше
- CSS не применяется — проверить путь к klein.css

- [ ] **Step 5: Commit после успешного запуска**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add modules_klein/ui_klein.py
git commit -m "feat: Klein UI first working launch on port 7861"
```

---

## Task 6: Styles Integration (styles.csv)

**Files:**
- Modify: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\ui_klein.py`

Реализует выбор стилей из `styles.csv`: multi-select теги + инъекцию в prompt/negative_prompt при генерации.

- [ ] **Step 1: Добавить загрузку styles.csv в ui_klein.py**

Добавить в начало `ui_klein.py` (после импортов):

```python
import csv

def _load_styles():
    """Load styles from styles.csv as list of (name, prompt, negative_prompt)."""
    csv_path = _os.path.join(
        _os.path.dirname(_os.path.dirname(__file__)),  # webui/
        'styles.csv'
    )
    styles = []
    if _os.path.exists(csv_path):
        with open(csv_path, newline='', encoding='utf-8') as f:
            reader = csv.DictReader(f)
            for row in reader:
                name = row.get('name', '').strip()
                if name:
                    styles.append({
                        'name': name,
                        'prompt': row.get('prompt', ''),
                        'negative_prompt': row.get('negative_prompt', ''),
                    })
    return styles

KLEIN_STYLES = _load_styles()
KLEIN_STYLE_NAMES = [s['name'] for s in KLEIN_STYLES]
```

- [ ] **Step 2: Добавить Styles UI в `_build_img2img_interface()` или config bar**

В `_assemble_klein_demo()` добавить Styles multi-select ДО рендера `img2img_interface`:

```python
# В _assemble_klein_demo(), после Model Bar, перед img2img_interface.render():

with gr.Row(elem_classes=["klein-config-bar"]):
    klein_styles_select = gr.Dropdown(
        choices=KLEIN_STYLE_NAMES,
        multiselect=True,
        label="Styles",
        elem_id="klein_styles_select",
        scale=4,
        container=False,
    )
    gr.HTML('<button class="klein-style-palette-btn" onclick="">🎨</button>')
```

- [ ] **Step 3: Добавить inject-callback для применения стилей**

Стили инъецируются в prompt/negative_prompt через `gr.State` и Python callback. Добавить в `_assemble_klein_demo()` после определения компонентов.

> **Важно:** `img2img_prompt` и `img2img_neg_prompt` — компоненты определённые внутри `img2img_interface` (рендерится через `.render()`). Их нужно передать через `gr.State` или сохранить как атрибуты перед рендером. Альтернатива — JS инъекция напрямую в поля по elem_id.

**Вариант A — JavaScript инъекция (рекомендуется, не требует доступа к компонентам):**

Добавить в `klein_js.py`:

```python
# NOTE: use a unique sentinel string (not '[]') — replaced once in ui_klein.py
STYLES_INJECT_JS = """
<script>
window._kleinStyles = __KLEIN_STYLES_JSON__;

function kleinApplyStyles(selectedNames) {
  const styles = window._kleinStyles;
  let addPrompt = '';
  let addNeg = '';
  selectedNames.forEach(name => {
    const s = styles.find(x => x.name === name);
    if (!s) return;
    if (s.prompt) addPrompt += (addPrompt ? ', ' : '') + s.prompt;
    if (s.negative_prompt) addNeg += (addNeg ? ', ' : '') + s.negative_prompt;
  });

  // Replace {prompt} placeholder or append
  const promptInput = document.querySelector('#img2img_prompt textarea');
  const negInput    = document.querySelector('#img2img_neg_prompt textarea');
  if (promptInput && addPrompt) {
    const base = promptInput.value;
    promptInput.value = addPrompt.replace('{prompt}', base) || (base + (base ? ', ' : '') + addPrompt);
    promptInput.dispatchEvent(new Event('input', {bubbles: true}));
  }
  if (negInput && addNeg) {
    const base = negInput.value;
    negInput.value = base + (base ? ', ' : '') + addNeg;
    negInput.dispatchEvent(new Event('input', {bubbles: true}));
  }
}
</script>
"""
```

В `ui_klein.py`, **заменить полностью** определение `_assemble_klein_demo()` из Task 4 Step 1 следующим.
Весь внутренний контент (Tabs, bottom nav HTML) остаётся без изменений — меняются только начало функции (head + config bar).

```python
def _assemble_klein_demo(img2img_interface, extras_interface):
    """Wrap img2img + extras in Klein shell with bottom nav and CSS."""
    # Bake styles data into JS once at build time
    styles_js = STYLES_INJECT_JS.replace("__KLEIN_STYLES_JSON__", _json.dumps(KLEIN_STYLES))
    head = canvas_head + ALL_JS + styles_js  # ← styles_js concatenated here

    with gr.Blocks(
        analytics_enabled=False,
        css=KLEIN_CSS,
        head=head,           # ← head now includes styles_js
        elem_classes=["klein-ui"],
        theme=gr.themes.Base(),
        title="Klein UI",
    ) as klein_demo:
        # Config bar with Styles dropdown + Apply button
        with gr.Row(elem_classes=["klein-config-bar"]):
            klein_styles_select = gr.Dropdown(
                choices=KLEIN_STYLE_NAMES,
                multiselect=True,
                label="Styles",
                elem_id="klein_styles_select",
                scale=4,
                container=False,
            )
            # Apply button reads selected values from the Gradio dropdown input
            # via JS: querySelectorAll on the internal checkbox inputs that
            # Gradio renders for multiselect dropdowns.
            gr.HTML(
                '<button class="klein-style-palette-btn" '
                'onclick="(function(){'
                'const sel=[];'
                'document.querySelectorAll(\'#klein_styles_select .item.selected span\').forEach(el=>sel.push(el.textContent.trim()));'
                'if(sel.length)kleinApplyStyles(sel);'
                '})()">🎨 Apply</button>'
            )
        # ... rest of _assemble_klein_demo (tabs, bottom nav) unchanged
```

- [ ] **Step 4: Smoke-тест styles injection**

```javascript
// В playwright smoke-тесте:
// 1. Убедиться что klein_styles_select dropdown отображается
const stylesDropdown = await page.locator('#klein_styles_select');
const visible = await stylesDropdown.isVisible();
console.log('Styles dropdown visible:', visible);
// 2. Выбрать первый стиль из списка
```

Ожидается: dropdown виден, при клике показывает список стилей из styles.csv.

- [ ] **Step 5: Commit**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add modules_klein/ui_klein.py modules_klein/klein_js.py
git commit -m "feat: Klein UI styles integration — styles.csv multi-select with JS injection"
```

---

## Task 7: Extras Tab + Mobile Polish

**Files:**
- Modify: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\ui_klein.py`
- Modify: `H:\Forge-Neo-v3.2-sage\webui\modules_klein\klein.css`

- [ ] **Step 1: Проверить что Extras tab работает**

```javascript
// Добавить в playwright smoke-тест:
const extrasTab = await page.locator('.klein-bottom-nav .klein-nav-item').nth(1);
await extrasTab.click();
await page.waitForTimeout(1000);
await page.screenshot({ path: 'C:/Users/krazz/AppData/Local/Temp/klein-extras.png' });
const upscalerVisible = await page.locator('text=Upscaler').first().isVisible();
console.log('Upscaler visible in Extras:', upscalerVisible);
```

- [ ] **Step 2: Мобильная проверка — sticky generate**

```javascript
// В playwright: мобильный viewport, прокрутка вниз, проверить что Generate всегда видна
await page.setViewportSize({ width: 390, height: 844 });
await page.goto('http://127.0.0.1:7861/');
await page.waitForTimeout(3000);
await page.evaluate(() => window.scrollTo(0, 500));
await page.screenshot({ path: 'C:/Users/krazz/AppData/Local/Temp/klein-sticky.png' });
```

Ожидается: кнопка Generate видна в нижней части экрана при скролле.

- [ ] **Step 3: Проверить collapsible model bar на мобильном**

```javascript
await page.setViewportSize({ width: 390, height: 844 });
// Model bar должен быть collapsed
const expandedBar = await page.locator('#klein-model-bar-expanded');
const display = await expandedBar.evaluate(el => el.style.display);
console.log('Model bar collapsed on mobile:', display === 'none');
// Тап для раскрытия
await page.locator('#klein-model-bar-toggle').click();
const displayAfter = await expandedBar.evaluate(el => el.style.display);
console.log('Model bar expanded after tap:', displayAfter !== 'none');
```

- [ ] **Step 4: Проверить resolution presets popup**

```javascript
await page.locator('#klein-res-btn').click();
await page.waitForTimeout(300);
const popupVisible = await page.locator('#klein-res-popup').isVisible();
console.log('Resolution popup visible:', popupVisible);
// Click 768x1344
await page.locator('#klein-res-popup button:has-text("768 × 1344")').click();
await page.waitForTimeout(500);
// Check width value
const widthVal = await page.locator('#img2img_width input[type=number]').inputValue();
console.log('Width after preset click:', widthVal); // expected: 768
```

- [ ] **Step 5: CSS adjustments**

Добавить в `klein.css` по результатам Playwright тестов любые необходимые правки.

- [ ] **Step 6: Final Playwright screenshot suite**

```javascript
// C:/Users/krazz/AppData/Local/Temp/playwright-test-klein-final.js
const { chromium } = require('playwright');
const OUT = 'C:/Users/krazz/AppData/Local/Temp/';

(async () => {
  const browser = await chromium.launch({ headless: false });
  const page = await browser.newPage();

  const viewports = [
    { name: 'mobile-390',  w: 390,  h: 844  },
    { name: 'tablet-768',  w: 768,  h: 1024 },
    { name: 'desktop-1280', w: 1280, h: 800 },
    { name: 'desktop-1920', w: 1920, h: 1080 },
  ];

  for (const vp of viewports) {
    await page.setViewportSize({ width: vp.w, height: vp.h });
    await page.goto('http://127.0.0.1:7861/', { waitUntil: 'load', timeout: 30000 });
    await page.waitForTimeout(3000);
    await page.screenshot({ path: `${OUT}klein-${vp.name}.png`, fullPage: false });
    console.log(`Saved: klein-${vp.name}.png`);
  }

  await browser.close();
  console.log('All viewport screenshots done');
})();
```

- [ ] **Step 7: Commit финальный**

```bash
cd "H:/Forge-Neo-v3.2-sage/webui"
git add modules_klein/ui_klein.py modules_klein/klein.css
git commit -m "feat: Klein UI mobile polish, sticky generate, resolution presets, extras tab"
```

---

## Task 8: End-to-End Verification

- [ ] **Step 1: Запустить оба webui одновременно**

```
# Терминал 1:
H:\Forge-Neo-v3.2-sage\RUN-Sage.bat    → http://192.168.2.57:7860/

# Терминал 2:
H:\Forge-Neo-v3.2-sage\RUN-Klein.bat   → http://192.168.2.57:7861/
```

Ожидается: оба запускаются без конфликтов, каждый на своём порту.

- [ ] **Step 2: Проверить Config Presets работают**

1. Открыть Klein UI на 7861
2. Выбрать пресет "Flux2 Klein Edit" в dropdown
3. Убедиться что Sampling Method, Steps, CFG устанавливаются автоматически

- [ ] **Step 3: Проверить LoRA загружается**

1. В Extra Networks найти вкладку Lora
2. Убедиться что LoRA файлы из `models/Lora/` отображаются

- [ ] **Step 4: Проверить расширения работают**

1. ControlNet accordion раскрывается
2. ADetailer accordion раскрывается
3. Стили из styles.csv применяются к промпту

- [ ] **Step 5: Проверить мобильный на реальном устройстве**

Открыть `http://192.168.2.57:7861/` на телефоне.
Проверить:
- Bottom nav работает
- Generate кнопка sticky при скролле
- Model bar collapsed
- Resolution popup открывается тапом

- [ ] **Step 6: Финальный commit**

```bash
cd "H:/Forge-Neo-v3.2-sage"
git add -A
git commit -m "feat: Klein UI v1 — mobile-optimized img2img interface on port 7861"
```

---

## Известные ограничения v1

- txt2img / PNG Info / Checkpoint Merger — не включены (v2)
- Setup API для Klein отдельно от основного (используется `--api` флаг)
- При одновременном запуске обоих webui — модель загружается в VRAM дважды
- Restart UI из интерфейса работает только если Klein запущен отдельно
